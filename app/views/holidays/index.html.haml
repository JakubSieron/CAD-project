- @holidays.each do |holiday|
	%h2= link_to holiday.name, holiday
	%p 
		Published at 
		= time_ago_in_words(holiday.created_at)
		by
		= holiday.name

= link_to "New Request", new_holiday_path