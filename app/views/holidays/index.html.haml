#holidays
	- @holidays.each do |holiday|
		.holiday
			%h2.name= link_to holiday.name, holiday
			%p.date 
				Published at 
				= time_ago_in_words(holiday.created_at)
				by
				= holiday.user.email

